CREATE TABLE courses (
  id INT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(100)
);
CREATE TABLE registrations (
  id INT PRIMARY KEY AUTO_INCREMENT,
  student_name VARCHAR(100),
  course_id INT,
  FOREIGN KEY (course_id) REFERENCES courses(id)
);
INSERT INTO courses (name) VALUES ('HTML/CSS'), ('PHP cơ bản');